# Dataset Structure

This project follows a standard ImageFolder-style dataset layout:

data/
train/
class_1/
class_2/
val/
class_1/
class_2/


Each subfolder corresponds to one Chinese character class.

Due to dataset size and licensing constraints, raw data is not included.

